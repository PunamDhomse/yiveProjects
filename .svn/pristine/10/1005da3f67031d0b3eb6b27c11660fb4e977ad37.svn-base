import { sidebarConstants } from '../_constants';

export function sidebar(state = false, action) {
  switch (action.type) {
    case sidebarConstants.SIDECLASS:
      return action.status
    default:
      return state
  }
}